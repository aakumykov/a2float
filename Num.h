class Num
{
  public:
    unsigned int a2long(char* char_array, int len);
    long a2long(char* char_array, int len);
    unsigned long a2long(char* char_array, int len);

  private:
    a2float(char* char_array, int len);
};
